# Cursor asset

Asset: `public/robot-cursor.png`.

Created once with the built-in image generation tool, using a transparent background. The returned 1254×1254 PNG was resized for delivery to 96×96 while preserving alpha. The website displays it at 36×36 CSS pixels.

Exact generation prompt:

```text
Use case: stylized-concept
Asset type: a single small cartoon robot sprite for a restrained academic personal website, positioned slightly bottom-right of the real cursor and displayed at 32–40 CSS pixels on a white page.
Primary request: create exactly one friendly compact robot with a rounded white/silver head and body, muted blue-gray accents, two simple dark eyes, tiny arms and tiny feet, and a cute silhouette that remains readable at very small size.
Scene/backdrop: genuinely transparent background with alpha. All pixels outside the robot must be transparent; do not render a checkerboard or solid background.
Style/medium: polished simple cartoon bitmap sprite with clean restrained shapes and enough dark blue-gray edge definition to read against a white homepage; minimal internal detail.
Composition/framing: square canvas; full single robot centered, filling the frame with a modest transparent margin; front view.
Color palette: white and soft silver with muted blue-gray accents.
Constraints: one robot only, genuine transparency, clear silhouette, no text, no lettering, no logo, no watermark, no glow, no particles, no scene, no gradient background, no shadow outside the silhouette.
```
