# 个人主页维护指南

这份文档说明如何修改标题、个人资料、论文、竞赛、荣誉、News、照片与简历，并将更新发布到网站。

你的网站是 **https://cybertime-cs.github.io/**，源代码仓库是 **https://github.com/cybertime-cs/cybertime-cs.github.io**。

## 先记住这三个位置

| 想修改什么 | 编辑哪个文件 |
| --- | --- |
| 栏目标题、导航文字、下载按钮文字、中英文提示语 | `lib/copy.ts` |
| 姓名、简介、邮箱、GitHub、学历、成绩、研究项目、论文、竞赛、荣誉、News | `content/profile.ts` |
| 照片和可下载 CV 文件 | `public/files/` |

一般不需要修改网页代码或样式。中英文内容都要分别填写；`zh` 表示中文，`en` 表示英文。

## 在 GitHub 网页上修改

1. 打开源码仓库，点开要修改的文件。
2. 使用文件编辑按钮进入编辑页面。
3. 只修改引号里面的文字，保留括号、冒号、逗号等结构。
4. 点击 **Commit changes** 保存，填写简短说明。
5. 在仓库 **Actions** 中查看 `Deploy academic homepage`，等待运行成功。
6. 刷新网站。如果仍显示旧内容，使用浏览器强制刷新。

绿色成功状态表示已部署。红色失败状态下，原来已发布的网站通常继续保留，可以打开失败步骤查看错误；也可以把错误发给我。

## 修改栏目标题和顶部导航

打开 `lib/copy.ts`。例如，想把中文正文标题“研究与实践”改成“科研经历”，修改 `zh` 内的：

```ts
research: '科研经历',
```

想同时把英文标题改为“Research Experience”，修改 `en` 内的：

```ts
research: 'Research Experience',
```

顶部导航使用独立的短文字，在相应语言的 `nav` 中修改：

```ts
nav: {
  about: '关于',
  education: '教育',
  research: '科研',
  publications: '论文',
  competitions: '竞赛',
  honors: '荣誉',
  news: '动态',
},
```

| 键名 | 对应栏目 |
| --- | --- |
| `education` | 教育与成绩 |
| `research` | 研究与项目总标题 |
| `researchExperience` | 研究经历小标题 |
| `projects` | 项目小标题 |
| `publications` | 论文发表 |
| `competitions` | 精选竞赛 |
| `honors` | 荣誉与奖励 |
| `news` | 底部 News |
| `coursework` | 核心课程提示 |
| `cvDownload` | CV 下载链接文字 |
| `publicationEmpty` | 没有论文记录时显示的文字 |

只改文字，不要改左侧键名。`research`、`honors` 这类代码键也用于网页内部定位，改标题不需要改网址或锚点。

## 修改姓名、简介和联系方式

在 `content/profile.ts` 的 `profile` 对象中编辑。例如：

```ts
name: { zh: '陶池邦', en: 'Chibang Tao' },
email: 'tcb@mail.nwpu.edu.cn',
secondaryEmail: 't3482379751@gmail.com',
github: 'https://github.com/cybertime-cs',
```

`introduction` 是简介段落列表。每一对 `zh/en` 对应一段，中英文段落可以自然翻译，不需要逐字直译。

```ts
introduction: [
  { zh: '第一段中文简介。', en: 'First paragraph in English.' },
  { zh: '第二段中文简介。', en: 'Second paragraph in English.' },
],
```

加入 Google Scholar、OpenReview 或 ORCID 账号时，把相应空字符串替换成自己的完整个人资料链接：`scholar`、`openreview`、`orcid`。保持为空时，页面不显示该链接。

## 修改教育、成绩和课程

- `education`：每所学校一条，包含 `school`、`degree`、`period` 和可选说明 `detail`。
- `metrics`：成绩条目。`label` 是名称，`value` 是数值；GPA 写清满分，例如 `3.98 / 4.1`，排名写清分母，例如 `3 / 173`。
- `courses`：核心课程列表，不必罗列所有课程。

现在的入学时间未填写，仅保留已知的预计毕业时间。补充年份时以真实记录为准。

## 修改研究经历、项目和技术能力

`experience` 中每条研究经历有机构名称 `title`、职责 `role` 和说明 `detail`。

`projects` 中每个项目包含：

| 字段 | 写法 |
| --- | --- |
| `title` | 项目中英文名称 |
| `description` | 两三句话说明做了什么、个人负责什么 |
| `url` | 可选的真实代码仓库或项目网页链接 |
| `role`、`period`、`tags` | 保留的结构化信息；当前简洁布局不单独显示 |

复制已有的完整项目对象即可新增一条。`skills` 用于页面末尾的编程和工程能力简述。

## 修改竞赛和荣誉

- `competitions` 是竞赛列表。
- `honors` 是奖学金和其他荣誉列表。

一条记录的结构如下，示例就是当前已核对的 RoboCup 记录：

```ts
{
  year: '2026',
  title: {
    zh: 'RoboCup 机器人大赛中国专项赛',
    en: 'RoboCup China Special Competition',
  },
  distinction: {
    zh: '国家一等奖 · 季军',
    en: 'National First Prize · Third Place',
  },
  organization: { zh: '', en: '' },
},
```

年份不确定时保持空字符串，不要猜测。页面会按数组顺序显示，不会自行推断日期。团队奖项和团队软件著作权应保留团队表述。

## 添加和更新 News

News 是底部的滚动区域。在 `news` 数组**最上方**插入最新一条，结构是“日期 + 一句中英文介绍”：

```ts
{
  date: 'YYYY-MM',
  text: {
    zh: '在这里写一句已经发生的新进展。',
    en: 'Write the same update in English.',
  },
  // url: '可选的成果链接',
},
```

上面的 `YYYY-MM` 是格式提示，需要替换成真实日期。例如 `2026`、`2026-09` 或 `2026-09-07`。不要把格式提示直接发布。

适合写入的内容包括论文接收、获奖、进入实验室、完成开源项目等。已写入的年份不代表具体月份。

News 的滚动区域高度在 `app/globals.css` 中：

```css
.news-scroll { height: 156px; width: 100%; }
```

例如改成 `220px` 可以一次显示更多动态。无需修改每条内容。

## 添加 Publications

`publications` 目前是空数组，网站会保留栏目并显示“目前暂无论文发表”。有真实论文后，在数组中加入完整记录：

```ts
{
  title: '正式论文题目',
  authors: '作者一, 作者二, Chibang Tao',
  venue: { zh: '会议或期刊名称', en: 'Venue name' },
  year: '真实年份',
  status: 'preprint',
  abstract: { zh: '中文摘要。', en: 'English abstract.' },
  // pdf: 'files/paper.pdf',
  // doi: '论文的完整 DOI 链接',
  // code: '真实代码仓库链接',
  // bibtex: `在这里粘贴正式 BibTeX`,
},
```

这只是字段示例，不是你的论文记录。按真实论文替换后再保存。

`status` 只能用以下三个值：

| 值 | 显示含义 |
| --- | --- |
| `published` | 已发表 |
| `preprint` | 预印本 |
| `submitted` | 投稿中 |

保留正式作者顺序。可选字段没有就不填。PDF 可用 `public/` 下的本地文件路径，也可用完整的公开网址。

## 替换照片

当前头像使用姓名缩写，没有使用原始简历照片。

1. 准备一张新照片，建议竖幅 JPG 或 PNG。
2. 上传到仓库 `public/files/`，例如文件名 `portrait.jpg`。
3. 在 `content/profile.ts` 修改为：

```ts
photo: 'files/portrait.jpg',
```

这里不写 `public/`。文件名区分大小写。头像展示比例由样式决定，不需要你手工裁成网页的确切大小。

## 更新 CV 下载文件

当前中英文 CV 分别位于：

- `public/files/cv-zh.pdf`
- `public/files/cv-en.pdf`

用新 PDF 覆盖同名文件，链接就无需修改。如果文件名变了，也要改 `content/profile.ts` 中的：

```ts
cv: { zh: 'files/cv-zh.pdf', en: 'files/cv-en.pdf' },
```

网页内容和 PDF 是两份文件，改网页不会自动更新 PDF。更新经历时请同步更新两者。原始 Word 简历没有放进公开网站；当前下载版没有照片、电话、出生年月等内容。

## 调整大白鼠标效果

图标是 `public/baymax-cursor.png`，使用大白的脸部造型，来自维基共享资源。网页版本为 96×96 像素，图形外部透明，脸部比例保持不变。它跟随在原生指针的右下侧，不影响点击。

- 显示尺寸在 `app/globals.css` 的 `#cursor-companion > img`，当前为 `36px`。
- 位置偏移由同一规则的 `translate(12px,12px)` 控制。
- 跟随逻辑在 `components/interactions.tsx`。
- 如要彻底关闭，在样式末尾加入 `#cursor-companion { display: none; }`。
- 触屏和系统“减少动画”模式自动隐藏，不必手动操作。

素材来源、原始 SVG 和小尺寸适配说明见 `docs/cursor-asset.md`。替换图标时，把新图片放入 `public/`；若文件名变化，请同步修改 `components/interactions.tsx` 中的 `sitePath('baymax-cursor.png')`。

## 本地修改并发布

在项目目录运行：

```sh
npm ci
npm run dev
```

本地中文地址 http://localhost:3000/ ，英文地址 http://localhost:3000/en/ 。检查修改后执行：

```sh
npm run build
npm run check
npm run lint
```

通过后提交并推送：

```sh
git add .
git commit -m "Update academic profile"
git push
```

GitHub Actions 会自动重新发布。`work/`、依赖和构建中间文件不会被提交；`public/` 中的文件会作为网站公开内容。

## 常见问题

**改了中文，英文为什么没变？**

两种语言独立维护。请同时修改 `zh` 和 `en`。

**为什么上传文件后是 404？**

确认文件在 `public/files/`，配置中只写 `files/文件名`，并检查大小写一致。再检查 Actions 是否部署成功。

**某个栏目暂时没有内容怎么办？**

Publications 可以保持空数组。其他栏目清空内容只会让该栏目没有条目；要完全隐藏栏目，需要调整 `components/academic-page.tsx` 中对应的 `<Section>`，并从顶部 `navigation` 数组移除同一个键。保留论文栏目符合当前网站需求。

**如何改变整个网站的字体、宽度或颜色？**

编辑 `app/globals.css`：`body` 控制正文字体，`.academic-page` 控制正文宽度，`a` 控制链接颜色，`h1/h2` 控制标题。普通内容更新不需要修改这些样式。

**修改时最容易出错的地方是什么？**

不要删除一条记录前后的 `{ }`，不要漏掉条目之间的逗号。英文中包含单引号时可改用双引号包裹字符串。修改前可复制当前记录作参照。
